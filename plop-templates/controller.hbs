// plop-templates/controller.hbs import {
{{properCase name}}Model } from "@/prisma/zod"; import { PrismaClient } from "@prisma/client"; const
prisma = new PrismaClient(); // TODO: 其他 CRUD 操作可仿造此处 stub 再补充 export async function
create{{properCase name}}(req, res) { const result =
{{properCase name}}Model .omit({ id: true, createdAt: true }) .safeParse(req.body); if
(!result.success) { return res.status(400).json({ error: result.error.format() }); } try { const
record = await prisma.{{name}}.create({ data: result.data }); res.status(201).json(record); } catch
(err) { res.status(500).json({ error: err.message }); } }